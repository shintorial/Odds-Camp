<nav class="bg-gray-800">
  <div class="container mx-auto px-4">
    <div class="flex items-center justify-between h-16">
      <div class="flex items-center">
        <%= link_to "Odds Camp", root_path, class: "text-white font-bold text-xl cursor-pointer" %>
      </div>
      <div class="flex items-center space-x-4">
        <% if user_signed_in? %>
          <%= link_to "New Post", new_post_path, class: "text-gray-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium cursor-pointer" %>
          <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "bg-red-500 hover:bg-red-600 text-white font-medium px-4 py-2 rounded-md text-sm transition-colors duration-200 cursor-pointer", data: { confirm: "Are you sure you want to sign out?" } %>
        <% else %>
          <%= link_to "Sign In", new_user_session_path, class: "text-gray-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium cursor-pointer" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "text-gray-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium cursor-pointer" %>
        <% end %>
      </div>
    </div>
  </div>
</nav> 