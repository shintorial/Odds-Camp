<nav class="navbar">
  <div class="navbar-container">
    <div class="navbar-content">
      <div class="navbar-brand">
        <%= link_to "Odds Camp", root_path, class: "navbar-logo" %>
      </div>
      <div class="navbar-actions">
        <% if user_signed_in? %>
          <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "navbar-button", data: { confirm: "Are you sure you want to sign out?" } %>
        <% else %>
          <%= link_to "Sign In", new_user_session_path, class: "navbar-link" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "navbar-link" %>
        <% end %>
      </div>
    </div>
  </div>
</nav> 